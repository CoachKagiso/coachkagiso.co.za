/**
 * Feature flags for toggling visibility of site sections.
 * Set to `false` to hide a section without deleting its code.
 */

export const FEATURE_FLAGS = {
  /** Toggle the Saturday Masterclass section site-wide */
  masterclass: false,
} as const;
